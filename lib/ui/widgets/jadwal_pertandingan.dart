import 'package:flutter/material.dart';

class JadwalPert extends StatefulWidget {
  const JadwalPert({Key? key}) : super(key: key);

  @override
  State<JadwalPert> createState() => _JadwalPertState();
}

class _JadwalPertState extends State<JadwalPert> {
  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
